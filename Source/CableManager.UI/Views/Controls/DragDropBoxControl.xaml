<UserControl x:Class="CableManager.UI.Views.Controls.DragDropBoxControl"
             x:Name="DragDropBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=DragDropBoxViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="65" d:DesignWidth="200">
   <StackPanel>
      <Border Width="200" Height="65" BorderThickness="1" CornerRadius="5,5,5,5" BorderBrush="Black" Background="White">
         <TextBlock Text="{Binding LabelProvider[UI_DragAndDrop]}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="Black" />
      </Border>
   </StackPanel>
</UserControl>